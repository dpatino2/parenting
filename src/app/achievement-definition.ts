import { Capability } from './capability';

export class AchievementDefinition {
    capability: Capability;
    level: number;
    description: string;
    iterations: number;
    image: string;
    skill: string;
}